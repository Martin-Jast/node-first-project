import React from 'react';
import ReactDOM from 'react-dom';
import { Controller } from './Controller.js';

//==================Rendering the view, passing data to react, etc.

  ReactDOM.render(
      <Controller />,
    document.getElementById('root')
  );
